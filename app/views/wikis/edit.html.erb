<h1>Edit Post</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for wikis</p>
     <ul>
       <li>Make sure it rhymes.</li>
       <li>Don't use the letter "A".</li>
       <li>The incessant use of hashtags will get you banned.</li>
     </ul>
   </div>
  <div class="col-md-8">
    <%= render partial: 'form', locals: { wiki: @wiki} %>
   </div>
  <div class="col-md-8">
    <%= render partial: 'dropdown'%>
   </div>
   <% @wiki.collaborators.each do |collaborator| %>
     <%= collaborator.user.email %>
     <%= link_to 'Remove', collaborator_path(collaborator), data: { confirm: 'Are you sure?' }, :style => "margin-left: 40px", method: :delete %>
   <% end %>
 </div>
